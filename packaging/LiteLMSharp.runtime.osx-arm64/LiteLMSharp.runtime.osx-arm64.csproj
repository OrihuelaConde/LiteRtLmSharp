<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Native-only NuGet package: LiteRT-LM macos-arm64 (.dylib) under runtimes/osx-arm64/native/.
    No managed assembly. Pair with the managed LiteLMSharp package (same version).
    libLiteRtLm.dylib links companions via -rpath @loader_path (same directory).
  -->
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <IncludeBuildOutput>false</IncludeBuildOutput>
    <IncludeSymbols>false</IncludeSymbols>
    <SuppressDependenciesWhenPacking>true</SuppressDependenciesWhenPacking>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <NoWarn>$(NoWarn);NU5128</NoWarn>

    <PackageId>LiteLMSharp.runtime.osx-arm64</PackageId>
    <Description>Native LiteRT-LM runtime (osx-arm64, Apple Silicon) for LiteLMSharp. Built from LiteRT-LM $(LiteRtLmVersion).</Description>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\runtimes\osx-arm64\native\*"
          Pack="true"
          PackagePath="runtimes\osx-arm64\native\" />
  </ItemGroup>

</Project>
